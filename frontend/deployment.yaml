apiVersion: apps/v1
kind: Deployment
metadata:
  name: frontend
  namespace: watchlist
spec:
  replicas: 2
  selector:
    matchLabels:
      app: frontend
  template:
    metadata:
      labels:
        app: frontend
    spec:
      containers:
        - name: frontend
          image: raj8875/watchlist-frontend:latest
          ports:
            - containerPort: 80
